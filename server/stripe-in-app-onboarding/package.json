{
  "name": "stripe-in-app-onboarding",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Account Session API for Stripe Connect onboarding inside the iOS app",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "stripe": "^18.5.0"
  }
}
